export default function SecurityCheck({ onVerified }) {
  return (
    <div
      style={{
        height: "100vh",
        background: "red",
        color: "white",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "24px",
      }}
    >
      SECURITY CHECK RENDERED
    </div>
  );
}
